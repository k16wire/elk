nginx:
  image: nginx
  ports:
   - 80
  volumes:
   - /home/docker/nginx.conf:/etc/nginx/nginx.conf
   - /home/docker/nginx/log:/var/log/nginx
   - /home/docker/nginx/content:/usr/share/nginx/html
logcollector:
  image: logstash
  external_links:
   - elk_redis_1:redis
  volumes:
   - /home/docker/nginx/log:/nginx/log
   - /home/docker/logstash:/config-dir
  command: logstash logstash -f /config-dir/log-collector.conf